// index.js

// If you rename `server.js` to `index.js`, simply add the content of `server.js` here:
const express = require('express');
// ... (rest of the server code)

app.listen(3000, () => {
  console.log('Server running on port 3000');
});
